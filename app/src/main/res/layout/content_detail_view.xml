<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.nick.accountant.DetailViewActivity"
    android:orientation="vertical"
    android:background="@color/colorBackground"
    tools:showIn="@layout/activity_detail_view">

    <ListView
        android:id="@+id/detail_view_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="15dp"
        android:background="@android:color/white"
        />

    <TextView
        android:id="@+id/detail_view_description"
        android:layout_alignTop="@id/detail_view_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:background="@android:color/white"
        android:elevation="10dp"
        android:textAlignment="center"
        android:text="description..."/>

    <Button
        android:id="@+id/detail_view_switch_button"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@color/colorPrimaryDark"
        android:textSize="15dp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:text="@string/detail_activity_balance_button"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:elevation="10dp"
        android:onClick="onChangeView"/>

</RelativeLayout>
